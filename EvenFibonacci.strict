has number
Sum Number
	mutable sum = 0
	for Range(1, number)
		next = first + second
		if next >= number
			return sum
		if (next % 2) is 0
			sum.Increment(next)
		first = second
		second = next
	sum