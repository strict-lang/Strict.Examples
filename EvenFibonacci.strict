has number
Sum Number
	constant sum = Mutable(0)
	for Range(1, number)
		next = first + second
		if next >= number
			return sum
		if (next % 2) is 0
			sum = sum + next
		first = second
		second = next
	sum